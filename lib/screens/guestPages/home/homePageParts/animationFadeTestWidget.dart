import 'package:flutter/cupertino.dart';

class AnimationFadeTestWidget extends StatelessWidget {
  const AnimationFadeTestWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
